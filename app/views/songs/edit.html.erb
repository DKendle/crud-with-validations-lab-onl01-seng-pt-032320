<%= form_for(@song) do |f|%>
    <%= f.title :title %>
    <%= f.released :released %>
    <%= f.release_year :release_year %>
    <%= f.artist_name :artist_name %>
    <%= f.genre :genre %>
    <%= f.submit%>
<% end %>